#include<iostream>
#include<algorithm>
using namespace std;
 
int main() {
 
	int a, b, c, n,arr[3];
	cin >> n;
 
 
	for (int i = 0; i < n; i++) {
		cin >> a >> b >> c;
		arr[0] = a;
		arr[1] = b;
		arr[2] = c;
 
		sort(arr, arr + 3);
		cout << arr[1]<<endl;
	};
 
 
	return 0;
}
